# Thronglets ServiceBus Configuration

# Server Configuration
THRONGLETS_HOST=localhost
THRONGLETS_PORT=8000
THRONGLETS_MCP_PATH=/bus/mcp

# Storage Configuration (memory or redis)
THRONGLETS_STORAGE_TYPE=memory
# THRONGLETS_REDIS_URL=redis://localhost:6379
# THRONGLETS_REDIS_DB=0
# THRONGLETS_REDIS_PREFIX=thronglets:

# ===========================================
# Authentication Configuration (Supabase JWT)
# ===========================================

# Enable authentication (default: false)
THRONGLETS_AUTH_ENABLED=false

# Supabase Project URL (required when auth is enabled)
# Example: https://abc123.supabase.co
THRONGLETS_SUPABASE_PROJECT_URL=

# Supabase Anon Key (required for frontend token generation)
# Found in Supabase Dashboard > Settings > API > anon public key
THRONGLETS_SUPABASE_ANON_KEY=
